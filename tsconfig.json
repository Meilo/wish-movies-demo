{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".", // This must be specified if "paths" is.
    "paths": {
      "core/*": ["src/core/*"],
      "ui/*": ["src/ui/*"],
      "config/*": ["*"]
    }
  }
}
