{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".", // This must be specified if "paths" is.
    "paths": {
      "domain/*": ["./src/domain/*"],
      "adapter/*": ["./src/adapter/*"],
      "view/*": ["./src/view/*"],
      "config/*": ["*"]
    }
  }
}
